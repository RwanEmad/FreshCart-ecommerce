import { FilterProByCatPipe } from './filter-pro-by-cat.pipe';

describe('FilterProByCatPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProByCatPipe();
    expect(pipe).toBeTruthy();
  });
});
